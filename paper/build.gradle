plugins {
    id 'java'
    id 'react.base-conventions'
    id 'react.shadow-conventions'
}

def dependenciesDir = file("${rootProject.projectDir}/dependencies")

dependencies {

    implementation project(":common")

    compileOnly "io.papermc.paper:paper-api:1.18.2-R0.1-SNAPSHOT"

    implementation fileTree(
            dir: dependenciesDir,
            include: [
                    'schedula-bukkit-1.0.0.jar',
            ]
    )

}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

sourceSets {
    main {
        resources {
            srcDir project(":common").file("src/main/resources")
        }
    }
}

processResources {
    inputs.property("mainClass", "com.g4vrk.react.paper.PaperReactPlugin")
    inputs.property("version", project.version)

    filesMatching("plugin.yml") {
        expand(
                version: project.version,
                mainClass: "com.g4vrk.react.paper.PaperReactPlugin"
        )
    }
}

jar {
    enabled = false
}

build {
    dependsOn shadowJar
}
