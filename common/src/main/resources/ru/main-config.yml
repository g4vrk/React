alerts:

  show-in-console: true

  format: "&c«React» &7| &f{player} - {check} {verbose}"

player:

  combat:
    time: 8s

  data:
    rotations-buffer-size: 150

actions:
  alerts:
    on-enable:
      - "[message] "
    on-disable:
      - "[message] "
